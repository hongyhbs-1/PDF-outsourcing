# dida985 报告排版 — 修改需求（交外包 longxiang · 2026-06-14）

> 委托方 = dida985 数据/报告侧 | 承接 = 外包排版工程师 longxiang
> 工作分支: **`dida985-baseline-20260614`**（本分支 = 委托方当前权威排版基线 golden_phase2）

## 〇、协作前提（重要）

1. **从本分支 `dida985-baseline-20260614` 起做**。请**勿**基于你的 `longxiang@4ae3e47`：委托方现用版已是其超集（同一套引擎 + 委托方侧封面老师位/引流/品牌/m5 等精修），并以委托方为准。你之前 longxiang 的版面改动**本轮不并入**。
2. **范围 = 仅模板/CSS 排版精修**。报告**数据由委托方注入**，你不需要、也不会拿到数据聚合/管线逻辑。你只需按下面"渲染字段"把数据排得好看。
3. 渲染技术栈不变：Py3.11 + Jinja2 + CSS（与你现有 `templates/` 一致）。
4. **本分支即开工包，无需再要文档/样本**：`dida985-handoff/` 内含 → 本需求文档 `REQUIREMENTS.md` + 渲染样本 `sample-payload.json` + 委托方草案模板 `draft-templates/`（2 个新段 jinja2+css）。拿到分支即可开工。

## 一、新增段 ①：冲刺方案（放**封面页后**）

- **位置**: 紧跟封面 `m0_cover` 之后、`m1_summary` 之前，作为全报告行动纲领。
- **段标题**: 冲刺方案。
- **渲染字段**: `remediation_plan`（见 `sample-payload.json`），结构：
  - `headline` — 子段「一句话总览」。
  - `priority_items[]` — 子段「**优先攻克高分集中点**」；每条：`knowledge_name` + `importance_level`（**文本星级**，如"重要（4星）"）+ `action`（结合错因的具体攻克动作）。按优先级排。
  - `difficulty_upgrade` = `{ intro, stages[]{ name, detail } }` — 子段「**难度升级建议**」；`stages` 用**阶段一 / 阶段二 / 集中冲刺**。
  - `stage_one_steps[]` = `{ step, detail }` — 子段「**一阶段重点**」；`step` 用**第一步 / 第二步 / …**。
- **风格**: 家长版，温暖专业、可读、不堆术语。
- ★ **铁律**:
  1. 不渲染任何分数（满分/得分）；重要度只用**文本"5星/4星"**，**不用 ★ 符号**。
  2. **不出现任何具体时间/周期**（如 30天 / 60天 / 一周 / 每天 / 本周）；推进节奏一律用 **阶段一 / 阶段二 / 集中冲刺** 与 **第一步 / 第二步** 表达。数据侧已按此口径产出，排版照渲即可。
- **草案参考**: `draft-templates/m_remediation_plan.jinja2` + `.css`（委托方草案版，按此精修；段内 class 前缀 `rp-`）。

## 二、新增段 ②：逐题错因分析（放**"7 逐题分析明细"之后** = 第8段·证据页）

- **位置**: 紧跟 `m9_question_detail`（"7 逐题分析明细"）之后，作为**证据页**的"逐题错因"。
- ★ **现有"第8段及之后"顺延后移一位**（TOC 编号 + 页序）。**此后移由你在精修时处理**。
- **渲染字段**: `per_question_causes[]`，每行列：
  `no`（题号）/ `knowledge`（知识点）/ `difficulty`（难度文本）/ `cause_l1`·`cause_l2`（错因大类·细类）/ `student_summary`（学生具体表现）/ `break_point`（首个突破点）。
- **只列错题**（有诊断的）；答对题不进此段。
- 表格/卡片版式由你定，要求：学生具体表现可能较长，需可读换行；证据页风格与既有 m9 协调。
- **草案参考**: `draft-templates/m_per_question_causes.jinja2` + `.css`（段内 class 前缀 `pqc-`）。

## ★ 全报告去时间化（具体时间 → 阶段）— 本轮新增要求

> 委托方口径：报告中所有「推进计划 / 节奏」文案 **不出现具体时间或周期**（N天 / N周 / 每天 / 每周 / 本周 / 每两周 / 坚持X天）；一律改为 **阶段一 / 阶段二 / 集中冲刺**（阶段）+ **第一步 / 第二步**（步骤）。

**需你改（模板硬编码，已定位）：**
- `pages/improvement_preview.jinja2`（提升预期）：三条提升路径与里程碑里全部 `约60天 / 约120天 / 约180天 / 0-60天 / 60-120天 / 120-180天 / 0-30天 / 30-60天`，以及 `每周1次 / 每两周1次 / 坚持180天 / 坚持60天 / 每60天` → 改为 **阶段一 / 阶段二 / 集中冲刺**；「每周 / 每两周」类节奏 → 「每阶段」或去掉。（该页已有「第一阶段」用法，统一沿用。）
- `pages/m6_tiered_learning.jinja2`（底部那句）：「建议按 **30/60 天**推进…**30 天**内先掌握…**60 天**内巩固…」 → 改为「按 **阶段一 / 阶段二** 推进…**阶段一**先掌握…**阶段二**巩固…」。
- `pages/m10_composition.jinja2`：「**本周**提升建议」「**本周**围绕…完成闭环训练」中的「本周」→「**本阶段**」。

**保留（不要动）：** 报告**事实性元数据**不算计划时间，保留原样 —— `m8_appendix` 的「生成日期 / 测评周期（96天）」、`test_period`（测评起止）、「近3个月 / 近4个月答题记录」、试卷月份。这些是数据来源说明，不是推进计划。

> ⚠️ 另有一类时间来自**数据（LLM 生成，如 m6 分层学习卡片 / 建议里的「每天 / 每周 / 1-2周」）**，不在模板里、你改不了 —— 由委托方改提示词处理，**你只需处理上面 3 个模板硬编码项**。

## 三、样本评审已定 + 待你定的排版细节

委托方样本评审结论（`sample-payload.json` 即据此）：
- ① 篇幅：冲刺方案建议**封面后约 1 页**；若超长，细清单可下沉到证据页区（**最终由你排版权衡**）。
- ② 列集：逐题错因保留 题号/知识点/难度/错因/学生表现；"突破点"列**可选**（偏内部，你可弱化或脚注）。
- ③ 仅错题入②。

## 四、交付 / 合并

- 在本分支精修模板/CSS，提交到本分支（或你的子分支）。委托方 **review 后并入** report `golden_phase2`。
- 委托方**随时可同步/合并**你的排版成果。
- 有渲染契约疑问（字段缺省、空值兜底）→ 在本仓 issue 或回委托方，**勿自行造数据逻辑**。
